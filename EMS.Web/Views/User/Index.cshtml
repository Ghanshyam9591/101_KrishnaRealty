
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .skin-black .right-side > .content-header {
        background: transparent;
        box-shadow: none;
        margin-left: -232px;
    }
</style>
<aside class="right-side">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            User Master
            <small>Preview</small>
        </h1>
        <div class="breadcrumb">
            <div class="mb-4"> <a href="/User/Item/0" class="btn btn-success float-right ml-1">+</a></div>
        </div>
    </section>
</aside>
<br />
<div ng-app="myApp" ng-controller="UserList">
    <div style="overflow:auto">
        <table class="table table-hover table-bordered">
            <thead>
                <tr style="background-color:darkgrey">
                    <th>SrNo</th>
                    <th>employee Code</th>
                    <th>employee Name</th>
                    <th>Email Address</th>
                    <th>Is Active</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="item in usergridData">
                    <td>{{$index+1}}</td>
                    <td><a href="/User/Item/{{item.seqid}}">{{item.employee_code}}</a></td>
                    <td>{{item.employee_name}}</td>
                    <td>{{item.email_address}}</td>
                    <td>{{item.is_active}} </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>